#!/bin/bash

# MassTransit cria automaticamente as filas dos consumers via ConfigureEndpoints.
# Este script é um no-op — mantido apenas para compatibilidade com o entrypoint do LocalStack.

echo "init-sqs.sh: filas gerenciadas pelo MassTransit — nenhuma acao necessaria."
