# State

> Decisions de M1–M8 arquivadas em [STATE-ARCHIVE.md](STATE-ARCHIVE.md)

## Decisions

- **2026-04-20 (Fase 11 M10 — README DONE):** mt-readme concluido. README.md reescrito para arquitetura MassTransit com 4 servicos: diagrama Mermaid atualizado (OrderService como orquestrador com state machine + EF Outbox, sem SagaOrchestrator), tabela de portas com 4 servicos .NET (porta 5002 removida), exemplos curl apontando para OrderService (5001) com /sagas/{id} em vez de porta 5002, secao de demos atualizada (estado "Final" da state machine vs Order.Status de negocio), secao de testes atualizada para 10 cenarios (T8/T9/T10 adicionados), secao de concorrencia reescrita para xmin + ConcurrencyMode.Optimistic, estrutura de pastas sem SagaOrchestrator/ e com Migrations/, observabilidade descrevendo AddSource("MassTransit"), docs didaticos com descricoes atualizadas. Checklist README da Fase 11 marcado como DONE.

- **2026-04-20 (Fase 10 M10 — DONE):** mt-demo-scripts concluida. `scripts/lib/common.sh`: removida variavel `ORCHESTRATOR_URL` (alias legado para saga-orchestrator); funcoes `poll_saga` e chamadas de saga agora usam `ORDER_URL` diretamente. `scripts/happy-path-demo.sh`: substituida referencia `ORCHESTRATOR_URL` por `ORDER_URL` em `assert_saga_terminal`. `scripts/concurrent-saga-demo.sh`: substituidas todas as referencias a `ORCHESTRATOR_URL` por `ORDER_URL`; textos de modo de lock atualizados de "COM LOCK (FOR UPDATE)"/"SELECT FOR UPDATE" para "COM CONCORRENCIA OTIMISTA (xmin + retry)" alinhados ao modelo Fase 4; comentario de cabecalho atualizado. Grep de residuos (`ORCHESTRATOR_URL|saga-orchestrator|FOR UPDATE|SELECT FOR UPDATE`) = 0 matches nos scripts.

- **2026-04-20 (Fase 9 M10 — DONE):** mt-integration-tests concluida. DockerComposeFixture e SagaClient ja estavam corretos (4 containers, porta 5001). Corrigidos bugs de assert em ConcurrencyTests e IdempotencyTests: estado terminal da state machine e sempre "Final" (nao "Completed"/"Failed") — Order.Status e que distingue resultado. Corrigido ConcurrencyTests para consultar Order.Status via GetOrderAsync em vez de contar por saga.State. Criado ResilienceTests.cs com 3 novos cenarios: T8 (resiliencia Outbox — pause/unpause saga-localstack 5s no meio da saga), T9 (concorrencia otimista — 10 sagas paralelas com estoque=10, todas completam via xmin retry), T10 (deduplicacao — 3 POST /orders rapidos geram 3 sagas distintas, todas completam sem colisao de CorrelationId). Build: 0 erros, 0 avisos. Total de cenarios: 7 existentes + 3 novos = 10 cenarios.
- **2026-04-20 (Fase 8 M10 — DONE):** mt-program-config concluida por validacao. Os 4 Program.cs estao no estado canonico: OrderService (75 linhas) tem AddMassTransit + AddSagaStateMachine + AddEntityFrameworkOutbox + MigrateAsync + MinimalAPI (GET /health, MapOrderEndpoints, MapSagaEndpoints); PaymentService (40 linhas) tem so consumers ProcessPayment + CancelPayment; InventoryService (71 linhas) tem so consumers ReserveInventory + CancelInventory; ShippingService (39 linhas) tem so ScheduleShippingConsumer. AddSource("MassTransit") esta centralizado em AddSagaTracing() em ServiceCollectionExtensions.cs — todos os 4 servicos o registram indiretamente. Nenhum HostedService<Worker> registrado em nenhum dos 4 servicos. Decisao sobre CancelShippingConsumer: o contrato CancelShipping.cs existe em Shared, porem o OrderStateMachine nunca envia esse comando durante compensacao — compensacao de shipping e no-op por design no estado atual. Se necessario futuramente, requer adicionar estado ShippingCancelling na state machine + consumer correspondente + evento ShippingCancelled (deferred para Deferred Ideas). Grep de residuos em Program.cs: 0 matches de Worker/SqsConfig/UseInMemoryOutbox/IdempotencyStore/HostedService<Worker>.
- **2026-04-20 (Fase 7 M10 — DONE):** mt-infra-docker concluida por re-validacao. Todos os itens ja estavam implementados antecipadamente nas Fases 1 e 6: `docker-compose.yml` com 4 servicos .NET (order, payment, inventory, shipping), `docker-compose.test.yml` idem com portas fixas 5001/5003/5004/5005, `init-sqs.sh` no-op (MassTransit cria filas via ConfigureEndpoints), `init.sql` apenas com uuid-ossp, health checks em `/health` para os 4 servicos, `.env` sem variaveis de orchestrator legado, `scripts/lib/common.sh` sem reply queues manuais. Grep de residuos: 0 matches em arquivos operacionais.
- **2026-04-20 (Fase 6 M10 — DONE):** mt-cleanup concluida. Removidos: `Shared/Configuration/SqsConfig.cs`, `Shared/Telemetry/SqsTracePropagation.cs`, `Shared/Contracts/Replies/*` (7 arquivos: BaseReply, PaymentReply, InventoryReply, ShippingReply, ReleaseInventoryReply, RefundPaymentReply, CancelShippingReply), `Shared/Contracts/Notifications/SagaTerminatedNotification.cs`. `infra/localstack/init-sqs.sh` simplificado para no-op (MassTransit cria filas automaticamente via ConfigureEndpoints). Mantidos: `BaseCommand.cs` (9 commands ativos herdam SagaId e Timestamp) e `ServiceCollectionExtensions.cs` (AddSagaConnectivity registra IAmazonSQS para SqsConnectivityCheck no /health — nao e despacho manual). Grep de residuos: 0 matches. Build: 0 erros, 2 avisos NU1903 pre-existentes.
- **2026-04-20 (Fase 5 M10 — DONE):** mt-idempotency concluida. Deletados `Shared/Idempotency/IdempotencyStore.cs` e `Shared/Models/IdempotencyRecord.cs`. Removido `using Shared.Idempotency` e `services.AddSingleton<IdempotencyStore>()` de `ServiceCollectionExtensions.cs`. Removida propriedade `IdempotencyKey` de `BaseCommand` (mantida a classe pois 9 commands ativos herdam `SagaId` e `Timestamp`). Tabela `idempotency_keys` nunca existia em producao (EnsureTableAsync nunca era invocado) — nenhuma migration de remocao necessaria. Grep `IdempotencyStore|IdempotencyKey|idempotency_keys` no src/ = 0 matches. Build: 0 erros, 2 avisos NU1903 (pre-existentes). Dedupe coberta por: correlacao saga (state machine ignora eventos em estados invalidos) + Outbox DuplicateDetectionWindow=30min (Fase 3).
- **2026-04-20 (Fase 4 M10 — DONE):** mt-concurrency concluida. `OrderSagaInstance` recebeu propriedade `uint xmin`. `OrderDbContext.OnModelCreating` configurou `entity.Property(e => e.xmin).HasColumnName("xmin").IsRowVersion()` — Npgsql mapeia automaticamente para coluna de sistema `xid` do PostgreSQL. `Program.cs`: `ConcurrencyMode` alterado de `Pessimistic` para `Optimistic`. Retry ajustado para 3 tentativas com backoff curto (100ms, 200ms, 500ms) adequado para conflitos de concorrencia otimista. Migration `AddXminConcurrency` gerada com `type: "xid"` e `rowVersion: true` — sem DDL extra em producao. Build: 0 erros, 2 avisos NU1903 (pre-existentes).
- **2026-04-20 (Fase 3 M10 — DONE):** Smoke test e criterio de DONE confirmados. Fase 3 encerrada formalmente. Proxima fase: mt-concurrency (Fase 4 — RowVersion + ConcurrencyMode.Optimistic).
- **2026-04-12 (Fase 3 M10):** mt-outbox-dlq concluida. `MassTransit.EntityFrameworkCore` ja estava presente no csproj. `OrderDbContext.OnModelCreating` recebeu `AddInboxStateEntity()`, `AddOutboxMessageEntity()`, `AddOutboxStateEntity()`. `Program.cs` recebeu `cfg.AddEntityFrameworkOutbox<OrderDbContext>` com `UsePostgres()`, `UseBusOutbox()`, `QueryDelay=1s`, `DuplicateDetectionWindow=30min`. `OrderStateMachineDefinition.ConfigureSaga` substituiu `sagaConfigurator.UseInMemoryOutbox(context)` por `endpointConfigurator.UseEntityFrameworkOutbox<OrderDbContext>(context)`. Migration `AddMassTransitOutbox` gerada — cria `InboxState`, `OutboxState`, `OutboxMessage` com indexes corretos. Endpoints DLQ e retry policy ja estavam tratados (Fase 1 e sqsCfg global). Build: 0 erros, 2 avisos NU1903 (pre-existentes). Pendente: teste smoke docker + criterio de DONE final (resiliencia simulada).
- **2026-04-12 (Fase 2 M10):** mt-db-migration concluida. Adicionado `Microsoft.EntityFrameworkCore.Design 10.0.0-preview.3.25171.6` ao OrderService.csproj (PrivateAssets=all — build-only). Gerada migration `InitialCreate` em `src/OrderService/Migrations/` (3 arquivos: migration, designer, snapshot) capturando schema de `orders` e `order_saga_instances`. `Program.cs` substituiu bloco `CreateTablesAsync` manual por `await db.Database.MigrateAsync()`. `infra/postgres/init.sql` ja estava correto (apenas uuid-ossp, sem DDL de tabelas). Build: 0 erros, 2 avisos NU1903 (vulnerabilidade em pacote transitivo `Microsoft.Build.Tasks.Core` do EF.Design — build tools only, nao afeta runtime).
- **2026-04-11 (Fase 1 M10):** mt-merge-order-orchestrator finalizado de verdade. Removidos: src/SagaOrchestrator/ (9 arquivos), entrada na solution, servico saga-orchestrator do docker-compose.yml, variavel SAGA_ORCHESTRATOR_PORT do .env, SAGA_ORCHESTRATOR_URL do order-service. Adicionado GET /sagas/{id} no OrderService (SagaEndpoints.cs) via OrderDbContext.SagaInstances. Tests/IntegrationTests atualizados: SagaClient unificado para porta 5001, SagaResponse.Transitions removido (MassTransit nao persiste historico de transicoes), assertes adaptados para estado "Final". DockerComposeFixture com 4 health checks. Scripts de demo e dashboard Grafana limpos. Build: 0 erros, 0 avisos.
- **2026-04-11:** Feature mt-messaging-infra concluida. T1 (pacote MassTransit.AmazonSQS 8.3.6) ja presente nos 4 csproj (OrderService, PaymentService, InventoryService, ShippingService). T2 `ConfigureSqsHost` extension em `Shared/Extensions/MassTransitSqsExtensions.cs` le AWS_SERVICE_URL/AWS_DEFAULT_REGION/AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY do IConfiguration e configura host via `amazonsqs://{region}` URI. Os 4 Program.cs ja usam `cfg.UsingAmazonSqs((context, sqsCfg) => { sqsCfg.ConfigureSqsHost(builder.Configuration); sqsCfg.UseMessageRetry(...); sqsCfg.ConfigureEndpoints(context); })` — trabalho antecipado durante mt-merge-order-orchestrator/mt-consumers. Env var do projeto e `AWS_SERVICE_URL` (nao `AWS_SQS_SERVICE_URL` como rascunhado na spec) — coerente com docker-compose.yml. Build 0 erros, 0 avisos. T3 (validacao runtime das filas no LocalStack) adiada para mt-infra-docker, onde docker-compose e init-sqs.sh serao revistos.
- **2026-04-11:** Feature mt-consumers concluida. Criados 5 consumers MassTransit (ProcessPaymentConsumer, CancelPaymentConsumer no PaymentService; ReserveInventoryConsumer, CancelInventoryConsumer no InventoryService; ScheduleShippingConsumer no ShippingService) + 5 ConsumerDefinitions com EndpointName especifico (process-payment, cancel-payment, reserve-inventory, cancel-inventory, schedule-shipping) e ConcurrentMessageLimit=4. Consumers usam context.Publish para emitir eventos tipados (PaymentCompleted/Failed/Cancelled, InventoryReserved/Failed/Cancelled, ShippingScheduled/Failed). ReserveInventoryConsumer preserva logica de INVENTORY_LOCKING_MODE (pessimistic/optimistic/none) reusando InventoryRepository. Workers.cs removidos dos 3 servicos. AddHostedService<Worker> removido dos 3 Program.cs, substituido por cfg.AddConsumer<T, TDefinition>(). Build da solution completo: 0 erros, 0 avisos.
- **2026-04-11:** Feature mt-state-machine concluida. OrderStateMachine reescrita com contratos tipados: substituidos PaymentReply/InventoryReply/ShippingReply/ReleaseInventoryReply/RefundPaymentReply por PaymentCompleted, PaymentFailed, InventoryReserved, InventoryFailed, ShippingScheduled, ShippingFailed, InventoryCancelled, PaymentCancelled. Correlacao migrada de SagaId para CorrelationId. Comandos de compensacao migrados para CancelPayment(CorrelationId, PaymentId) e CancelInventory(CorrelationId, ReservationId). Build 0 erros, 0 avisos.
- **2026-04-11:** Feature mt-event-contracts concluida. 14 contratos MassTransit criados em Shared/Contracts/Events/ (OrderPlaced, OrderItem, PaymentCompleted, PaymentFailed, PaymentCancelled, InventoryReserved, InventoryFailed, InventoryCancelled, ShippingScheduled, ShippingFailed) e Commands/ (ProcessPayment, ReserveInventory, ScheduleShipping com CorrelationId + backward compat com BaseCommand; CancelPayment, CancelInventory, ShippingAddress novos; InventoryItem extraido para arquivo proprio). Contratos antigos mantidos temporariamente (remocao via mt-cleanup). Build solucao completa com 0 erros.
- **2026-04-11:** Feature mt-merge-order-orchestrator concluida. OrderService absorveu a responsabilidade de orquestracao: HTTP hop para SagaOrchestrator removido, Worker.cs (polling order-status-updates) removido, endpoints movidos para Api/OrderEndpoints.cs, OrderDbContext unificado com DbSet<OrderSagaInstance>, MassTransit adicionado (in-memory por ora — SQS transport vem em mt-messaging-infra). Evento OrderPlaced criado em Shared/Contracts/Events/. Build compila com 0 erros.
- **2026-04-04:** Milestone M10 (Migração MassTransit) criada no ROADMAP com 18 features. Decisao arquitetural: fundir OrderService + SagaOrchestrator em um unico OrderService. Justificativa: com MassTransit a state machine e declarativa (~1 classe), nao justifica servico dedicado; elimina HTTP hop anti-pattern (POST /sagas); resolve bug Order.Status preso em Processing (M9 tornado obsoleto); Order e Saga sao do mesmo bounded context (order fulfillment). Resultado: 4 microsservicos independentes (OrderService, PaymentService, InventoryService, ShippingService) em vez de 5. Escopo total: ~900+ linhas eliminadas, 9+ filas SQS removidas, dual-write resolvido, docs reescritos (8 existentes + 2 novos), 7 docs codebase.

## Blockers

_Nenhum no momento._

## Lessons Learned

- **LocalStack >=2026.3:** Requer `LOCALSTACK_ACKNOWLEDGE_ACCOUNT_REQUIREMENT=1` para rodar sem auth token (snooze temporario)
- **.NET 10 preview aspnet image:** Nao inclui `curl` nem `wget`. Necessario instalar `curl` via apt no Dockerfile para health checks do Docker Compose
- **.NET 10 csproj:** Precisa de `<ImplicitUsings>enable</ImplicitUsings>` para `WebApplication` funcionar sem `using` explicito
- **AWSSDK.SQS v4 preview:** Versao 4.0.0-preview.5 e necessaria para .NET 10 — versao 3.x nao e compativel
- **EF Core 10 preview:** Usar 10.0.0-preview.3.25171.6. Npgsql.EFCore 10.0.0-preview.3 e compativel
- **Dockerfile com Shared:** Build context precisa ser raiz do repo (nao src/Service) para copiar Directory.Build.props e Shared
- **OpenTelemetry 1.15.0 + .NET 10:** OTel 1.15.0 depende de Microsoft.Extensions 10.0.0 (estavel). Versoes preview causam NU1605
- **SQS trace propagation manual:** AWSSDK.SQS nao tem instrumentacao OTel nativa. Necessario inject/extract manual de traceparent/tracestate via MessageAttributes
- **Task.WhenAll no SQS worker:** Para expor race conditions, o worker precisa processar mensagens do mesmo batch em paralelo
- **Delay artificial como janela TOCTOU:** Task.Delay(150ms) entre SELECT e UPDATE (modo sem lock) garante que multiplas transacoes paralelas vejam o mesmo valor
- **EF Core Design NU1903:** `Microsoft.EntityFrameworkCore.Design` puxa `Microsoft.Build.Tasks.Core` que tem vulnerabilidade de severidade alta (GHSA-h4j7-5rxr-p4wc). E build-time only (PrivateAssets=all) e nao afeta runtime. Warning esperado e inevitavel enquanto EF.Design for dependencia.
- **inventory_reservations como ponte de compensacao:** Armazenar reservation_id permite que compensacao restaure exatamente o estoque correto

## Deferred Ideas

_Ver "Future Considerations" no ROADMAP.md._

## Preferences

- Documentacao em portugues
- Construcao incremental por milestones
- Usar subagentes para trabalho pesado, manter janela de tokens baixa
